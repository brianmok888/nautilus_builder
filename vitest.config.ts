// Root vitest config placeholder.
// Vitest must be run from apps/web/ where the full config and dependencies live:
//   cd apps/web && npx vitest run
//
// Running from repo root without vitest installed will fail.
// See apps/web/vitest.config.mts for the actual configuration.
export default {};
