export const StrategySpecEditor = () => {
  return (
    <section aria-label="strategy spec editor">
      <h3>StrategySpec editor</h3>
      <p>Draft JSON/YAML preview remains subject to backend validation and lifecycle gates.</p>
    </section>
  );
};
